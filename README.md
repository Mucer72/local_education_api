The first version of the backend server system for the Local Education application.

Built using Python Flask. This project is no longer suitable for the application due to time constraints and the application's required functionality.
